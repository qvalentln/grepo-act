version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: grepo_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: grepotracker
      POSTGRES_USER: grepo_user
      POSTGRES_PASSWORD: grepolis
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  pgdata: